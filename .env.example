# 微博 Cookie（从浏览器 F12 → Application → Cookies → weibo.com 复制）
# 核心字段是 SUB=xxx; SUBP=xxx; 其他字段也建议一起复制
WEIBO_COOKIE=SUB=替换为你的SUB值; SUBP=替换为你的SUBP值;

# 发博内容配置（可选，如果用 scheduler.py 的话）
# 每行一条微博内容，脚本随机选一条发送
# 或者你可以直接在 scheduler.py 里面改
